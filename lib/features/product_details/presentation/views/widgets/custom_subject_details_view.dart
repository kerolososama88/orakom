import 'package:flutter/material.dart';

class CustomSubjectDetailsView extends StatelessWidget {
  const CustomSubjectDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
