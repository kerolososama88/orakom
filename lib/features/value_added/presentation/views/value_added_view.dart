import 'package:flutter/material.dart';
import 'package:orakom/features/value_added/presentation/views/widgets/value_added_body.dart';

class ValueAddedView extends StatelessWidget {
  const ValueAddedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ValueAddedBody(),
    );
  }
}
